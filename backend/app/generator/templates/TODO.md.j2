# TODO — {{ config.project_name }}

## Next Steps

- Review `tools.py` and replace stub implementations with real integrations.
- Confirm deterministic tools remain direct Python calls in `agent.py`.
- Extend `callbacks.py` with audit logging and guardrails.
- Validate `/health`, `/run`, and `/stream` in `api_server.py`.
- Add environment secrets to `.env` before deployment.
